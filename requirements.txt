# parselmouth must be installed from source to support "To Pitch (filtered ac)"
# pip install praat-parselmouth>=0.4.3 will NOT work for this pipeline
# Use the following instead:
# pip install git+https://github.com/YannickJadoul/Parselmouth.git --config-settings="--build-option=--cmake-executable=/usr/bin/cmake"

textgrid>=1.5
pandas>=1.3
numpy>=1.21
matplotlib>=3.4
